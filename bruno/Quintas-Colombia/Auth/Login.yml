info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/login
  body:
    type: json
    data: |-
      // Admin
      {
          "email": "nilson@correo.co",
          "password": "1234567890"
      }
      // Propietario/Cliente
      // {
      //     "email": "zinedine@correo.co",
      //     "password": "1234567890"
      // }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
